#include <iostream>
#include <thread>
#include <string>
#include <map>
#include <vector>
#include "server_loop.h"
#include "packets/packet.h"

using std::vector;
using std::string;

namespace radius {

TEST_CASE("A test", "[A tag]") { REQUIRE(true == true); }
}
